<main>
  {{!-- Hero Section --}}
  <section class="hero-section">
    <div class="container">
      <div class="hero-content">
        <h1>Fresh &amp; Organic<br><span>Delivered to You</span></h1>
        <p>Farm-fresh fruits, vegetables and organic groceries delivered straight from the farm to your doorstep. 100% organic and chemical free.</p>
        <div class="hero-cta">
          <a href="/v1/auth/register" class="btn-primary-custom">
            Shop Now <i class="fas fa-arrow-right"></i>
          </a>
          <a href="/about-use" class="btn-outline-custom">
            Learn More <i class="fas fa-leaf"></i>
          </a>
        </div>
      </div>
      <div class="hero-image">
        <div class="hero-image-placeholder">
          <i class="fas fa-seedling"></i>
        </div>
      </div>
    </div>
  </section>

  {{!-- Features Bar --}}
  <section class="features-bar">
    <div class="container">
      <div class="feature-item">
        <div class="feature-icon"><i class="fas fa-truck"></i></div>
        <div>
          <h4>Free Shipping</h4>
          <p>On all orders within 2km</p>
        </div>
      </div>
      <div class="feature-item">
        <div class="feature-icon"><i class="fas fa-undo-alt"></i></div>
        <div>
          <h4>Free Returns</h4>
          <p>Returns are free within 9 days</p>
        </div>
      </div>
      <div class="feature-item">
        <div class="feature-icon"><i class="fas fa-shield-alt"></i></div>
        <div>
          <h4>100% Secure</h4>
          <p>Your payment is safe with us</p>
        </div>
      </div>
      <div class="feature-item">
        <div class="feature-icon"><i class="fas fa-headset"></i></div>
        <div>
          <h4>Support 24/7</h4>
          <p>Contact us 24 hours a day</p>
        </div>
      </div>
    </div>
  </section>

  {{#if products}}
  {{!-- Best Sellers --}}
  <section class="products-section">
    <div class="container">
      <div class="section-header">
        <h2>Best Sellers</h2>
        <p>Add bestselling products to your weekly line up</p>
      </div>
      <div class="product-grid" id="bestseller-grid">
        {{#each products}}
        <div class="product-card">
          <a href="/view-product/{{this._id}}" class="card-img">
            {{#if this.img_path.[0]}}
            <img src="/product-images/{{this.img_path.[0]}}" alt="{{this.title}}">
            {{else}}
            <i class="fas fa-image" style="font-size:2rem;color:#ccc;"></i>
            {{/if}}
            <span class="card-badge">New</span>
          </a>
          <div class="card-body">
            <h3><a href="/view-product/{{this._id}}">{{this.title}}</a></h3>
            <div class="card-price">
              <span class="current"><i class="fas fa-rupee-sign" style="font-size:0.8em;"></i> {{this.actualprice}}</span>
              {{#if this.price}}<span class="original"><i class="fas fa-rupee-sign" style="font-size:0.8em;"></i> {{this.price}}</span>{{/if}}
            </div>
            <div class="card-actions">
              <button class="btn-add-cart" onclick="addToCart('{{this._id}}')">
                <i class="fas fa-shopping-cart"></i> Add to cart
              </button>
            </div>
          </div>
        </div>
        {{/each}}
      </div>
    </div>
  </section>
  {{/if}}

  {{!-- Banner Strip --}}
  <section class="banner-strip">
    <div class="container">
      <div class="banner-item">
        <img src="/banner/chips.jpg" alt="Snacks">
      </div>
      <div class="banner-item">
        <img src="/banner/icecream.jpg" alt="Ice Cream">
      </div>
      <div class="banner-item">
        <img src="/banner/almonds.jpg" alt="Dry Fruits">
      </div>
    </div>
  </section>

  {{!-- Newsletter --}}
  <section class="newsletter-section">
    <div class="container">
      <h2>Stay in the Loop</h2>
      <p>Get notified about new products, offers, and organic tips straight to your inbox.</p>
      <form class="newsletter-form" onsubmit="event.preventDefault();Swal.fire('Coming Soon!','Newsletter subscription will be available soon.','info')">
        <input type="email" placeholder="Enter your email address" required>
        <button type="submit">Subscribe</button>
      </form>
    </div>
  </section>
</main>

<input type="hidden" id="orderId" value="{{deliveryStatus}}">
<input type="hidden" id="deliveryStatus" value="{{delivery}}">

<script>
  function addToCart(prdId) {
    $.ajax({
      url: '/add-to-cart/' + prdId,
      method: 'get',
      success: (response) => {
        if (response.status) {
          let count = $('#cartCount').html()
          count = parseInt(count) + 1
          $('#cartCount').html(count)
        }
      }
    })
  }
</script>

<script>
  $(document).ready(async function () {
    let delivery = document.getElementById('deliveryStatus').value
    let deliveryStatus = document.getElementById('orderId').value
    if (delivery == 'true') {
      const { value: text } = await Swal.fire({
        input: 'textarea',
        inputLabel: 'Hope you are happy with our product...',
        inputPlaceholder: 'Type your feedback here...',
        inputAttributes: { 'aria-label': 'Type your message here' },
        showCancelButton: false
      })
      if (text) {
        $.ajax({
          url: '/save-textfeedback',
          method: 'post',
          data: { feedback: text },
          success: (response) => {
            if (response.feedbackSatatus) {
              Swal.fire('Thank you for your valuable feedback')
            }
          }
        })
      } else {
        $.ajax({
          url: '/save-textfeedback',
          method: 'post',
          data: { feedback: 'No Feedback' },
          success: (response) => {
            if (response.feedbackSatatus) {
              Swal.fire('Thank you for your valuable feedback')
            }
          }
        })
      }
    }
  })
</script>